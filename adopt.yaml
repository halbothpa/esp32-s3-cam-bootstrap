# Generated locally in your browser. This file contains key names only.
substitutions:
  device_name: esp32-s3-cam
  friendly_name: ESP32-S3 Camera

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  min_version: 2026.7.4

packages:
  private_camera:
    url: https://github.com/halbothpa/esp32-s3-cam-esphome.git
    ref: stable
    files:
      - packages/camera.yaml
    username: !secret esp32_s3_cam_github_username
    password: !secret esp32_s3_cam_github_token
    refresh: 1d

external_components:
  - source:
      type: git
      url: https://github.com/halbothpa/esp32-s3-cam-esphome.git
      ref: stable
      username: !secret esp32_s3_cam_github_username
      password: !secret esp32_s3_cam_github_token
    components:
      - sd_snapshot
    refresh: 1d

api:
  encryption:
    key: !secret api_encryption_key
  reboot_timeout: 15min

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  ap:
    ssid: "${friendly_name} Fallback"
    password: !secret fallback_ap_password

captive_portal:

web_server:
  port: 80
  version: 3
  local: true
  ota: false
  auth:
    username: !secret web_username
    password: !secret web_password
    type: digest
